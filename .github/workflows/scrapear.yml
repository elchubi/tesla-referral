name: Descargar imágenes Tesla oficiales

on:
  schedule:
    - cron: '0 0 1 * *'  # Ejecutar el día 1 de cada mes a las 00:00 UTC
  workflow_dispatch:

jobs:
  scrape_gallery:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repositorio
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: pip install beautifulsoup4 requests

      - name: Ejecutar descarga desde galería oficial
        run: python scripts/scrapear_tesla.py

      - name: Commit imágenes nuevas
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@users.noreply.github.com"
          git add assets
          git commit -m "📸 Nuevas imágenes descargadas desde galería oficial de Tesla" || echo "Sin cambios"
          git push
